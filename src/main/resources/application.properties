spring.application.name=demo

# DataSource 基本設定
# spring.datasource.url=jdbc:mysql://localhost:3306/myjavapj_aws?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=m05140210
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#雲端資料庫連線設定
# spring.datasource.url=jdbc:mysql://aws-db-spring.cqd0q4sws9om.us-east-1.rds.amazonaws.com:3306/myjavapj_aws
# spring.datasource.username=admin
# spring.datasource.password=m05140210

# 使用環境變數，EB 會在執行時自動注入這些值
spring.datasource.url=jdbc:mysql://${RDS_HOSTNAME}:${RDS_PORT}/${RDS_DB_NAME}
spring.datasource.username=${RDS_USERNAME}
spring.datasource.password=${RDS_PASSWORD}

#JPA設定
spring.jpa.hibernate.ddl-auto=update
#這段是因為要上雲端加的
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG

#SQL欄位大小寫轉換，只要資料庫有駝峰命名，就要記得加
#spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

server.port=5000